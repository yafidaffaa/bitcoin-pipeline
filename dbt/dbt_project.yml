name: 'bitcoin_pipeline'
version: '1.0.0'

profile: 'bitcoin_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bitcoin_pipeline:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart